# 少女温柔的抚摸

| 字段 | 值 |
|------|-----|
| **ID** | — |
| **资源 Key** | `maiden_touch` |
| **中文名** | 少女温柔的抚摸 |
| **解锁** | 大力士剧情（设计）；**已禁用** 🚫 |

---

## 效果

向鼠标方向释放抓取区域（设计未完成）。

- 等级1～4：升级逻辑复制自激光剑类武器，**引用未定义变量**，运行时会报错

---

## 数值

| 参数 | 代码状态 |
|------|----------|
| `baseDamage` | **未初始化** |
| 攻击间隔 | 1.2s |
| 基础暴击率 | 5% |
| 基础暴击伤害 | 200% |

---

## 代码 ↔ 文档差异

- 列入 `GameInfo.DISABLED_WEAPONS`，不进商店池。
- `upgrade()` 使用 `slashScale`、`anis`、`pierceCritRate` 等 **未声明字段**；`attack()` 未使用 `calculate_damage()` 结果。
- 场景未配置 `id`、`na`、`descriptions`。
- 关联大力士 3000 杀剧情，解锁事件 **未接入**。
